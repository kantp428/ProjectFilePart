package AllUser;

public class Admin extends User{

    public Admin(String userName, String fullName, String lastName, String password, String studentID,AllUser allUser) {
        super(userName, fullName, lastName, password, studentID,allUser);
    }
}
